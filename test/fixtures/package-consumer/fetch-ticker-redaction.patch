diff --git a/src/handlers/execute-action/pass-through.ts b/src/handlers/execute-action/pass-through.ts
index f082774547abc9d7ac89e4019040e9127fcad556..aaa43ccd678b1c008a2e72d21b9e7a2ecc32eacd 100644
--- a/src/handlers/execute-action/pass-through.ts
+++ b/src/handlers/execute-action/pass-through.ts
@@ -6,11 +6,16 @@ import {
 	marketTypeToCcxtType,
 	parseMarketType,
 } from "../../helpers/market-type";
-import { getErrorMessage, safeLogError } from "../../helpers/shared/errors";
+import {
+	getErrorMessage,
+	safeLogError,
+	safeLogRedactedError,
+} from "../../helpers/shared/errors";
 import {
 	resolveTransferNetwork,
 	type TransferNetworkResolution,
 } from "../../helpers/transfer-network";
+import { sanitizeVenueError } from "../../helpers/venue-evidence";
 import { FetchDepositAddressesPayloadSchema } from "../../schemas/action-payloads";
 import type { ExecuteActionContext } from "./context";
 import { parsePayloadForAction } from "./context";
@@ -231,7 +236,10 @@ async function handleFetchTicker(ctx: ExecuteActionContext): Promise<void> {
 			result: JSON.stringify(ticker),
 		});
 	} catch (error) {
-		safeLogError(`Error fetching ticker from ${cex}`, error);
+		safeLogRedactedError(
+			`Error fetching ticker from ${cex}`,
+			sanitizeVenueError(error, broker),
+		);
 		ctx.wrappedCallback(
 			{
 				code: grpc.status.INTERNAL,
