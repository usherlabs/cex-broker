## 1. Characterization and Contract Tests

- [x] 1.1 Add focused supervisor tests proving concurrent matching public subscriptions share one worker/watch/archive cadence and that exchange normalization, resolved symbol, market type, feed, ORDERBOOK acquisition profile, and OHLCV resolved timeframe split canonical keys while credentials, account selector, raw depth, and bootstrap limit do not.
- [x] 1.2 Add ORDERBOOK resolver contract tests for stable profile IDs, guaranteed retained depth, archive-depth inclusion, explicit/omitted projection metadata, incompatible later-depth isolation, conservative exact/default fallback, and the fact that a nominal top-N count alone does not prove Maker price-band coverage.
- [x] 1.3 Characterize and test Binance and MEXC CCXT watch behavior, then prove compatible explicit/omitted depths map to one coalesced public L2 acquisition profile without passing arbitrary projection depth as physical identity.
- [x] 1.4 Add public broker-selection and archive-row tests covering configured-primary precedence, ignored secondary selection, primary-label `account_selector`, omitted selector for request-created/credentialless workers, checksum fixture impact, and configured versus worker-owned exchange cleanup.
- [x] 1.5 Add subscriber-buffer tests for FIFO behavior, protobuf-wire byte measurement, injected count and byte limits, empty-buffer oversized-frame failure, exact `Public market-data subscriber fell behind` terminal JSON, isolated RPC termination, safe overflow metric labels, and continued archive/healthy-subscriber delivery.
- [x] 1.6 Add worker lifecycle tests covering one-of-many unsubscribe, request-created exchange lifetime beyond its creating RPC, final-subscriber atomic retirement/close-once, configured-primary unwatch and no-unwatch barriers, subscribe-during-retirement, ignored late watch results, terminal error fanout, fresh worker after failure, and bounded retirement timeout.
- [x] 1.7 Add feed tests for normalized L2 `[price, amount]` immediate-hedgeability evidence without L3 identity, base ORDERBOOK archive before subscriber slicing, existing archive depth cap and `sampled_top_n_snapshot`, explicit/legacy/omitted `depthLimit` metadata, late-join waits for next observation, and archive independence from gRPC drain.
- [x] 1.8 Add OHLCV tests for zero-bootstrap first attach, atomic first-positive ownership, failed-fetch release/retry, completed-bootstrap collector restart, subscriber-local later bootstrap ordering before live frames, later-fetch failure, and no tracker/archive mutation.
- [x] 1.9 Add runtime lifecycle and transport tests for `run()` replacement, awaited `stop()` cleanup, configured-primary preservation, request-created close, bare close without blocked drain, and bare close during blocked drain.

## 2. Public Feed Supervisor and Acquisition Profiles

- [x] 2.1 Implement a helper-level fixed-capacity O(1) ring buffer that tracks frame count and full protobuf-wire-encoded `SubscribeResponse` bytes, supports close/fail wakeups, and exposes test-only capacity overrides while retaining 16-frame and 1 MiB production constants.
- [x] 2.2 Implement canonical public feed key construction with `trim().toLowerCase()` exchange normalization, `1m` OHLCV default timeframe, and an ORDERBOOK acquisition-profile component rather than raw subscriber depth.
- [x] 2.3 Implement the ORDERBOOK resolver interface returning stable profile ID, upstream CCXT limit/options, guaranteed retained depth, and coalescing support; add Binance and MEXC candidate resolvers plus conservative explicit/default fallback.
- [x] 2.4 Implement public exchange resolution and ownership metadata with primary/request/credentialless precedence, worker-scoped request-created lifetime, configured-primary preservation, and stable public archive `account_selector` selection.
- [x] 2.5 Implement the runtime-scoped public feed registry, atomic worker get-or-create, closeable async-iterable subscribers, archive-decision-before-fanout sequencing, FIFO fanout, and subscriber-isolated overflow handling with the specified terminal error.
- [x] 2.6 Implement ORDERBOOK workers that acquire the resolved L2 profile, preserve ordered aggregate price/amount levels in one retained base snapshot, run one archive decision with the configured archive depth cap, and project independent explicit/legacy/omitted responses without hot-swapping an active profile or synthesizing L3 identity.
- [x] 2.7 Implement TICKER and TRADES workers with one watcher/archive owner per canonical key and next-observation-only late-join behavior.
- [x] 2.8 Implement OHLCV workers with one live tracker, `unclaimed`/`in-flight`/`complete` archive-bootstrap state, non-claiming zero requests, retry after failure, serialized subscriber-local later bootstrap, and shared live delivery.
- [x] 2.9 Implement generation-fenced failure/retirement, optional feed-specific `unWatch*`, configured-primary predecessor barriers and timeout, late-result suppression, worker-owned exchange close-once, and supervisor-wide awaited shutdown.
- [x] 2.10 Add credential-free physical-worker, profile, physical-frame/archive-decision, active-subscriber, and overflow telemetry without API keys, account selectors, or credential-derived labels.

## 3. Subscribe and Runtime Integration

- [x] 3.1 Inject the concrete public feed supervisor through `CEXBroker` and `getServer` into Subscribe dependencies while keeping `src/server.ts` limited to handler wiring and helpers independent of handlers/server.
- [x] 3.2 Replace only the public ORDERBOOK, TICKER, TRADES, and OHLCV per-RPC loops with supervisor subscriptions, preserving request validation, response JSON/protobuf shape, terminal error frames, legacy `NO_ACTION`, and subscriber-local gRPC drain handling.
- [x] 3.3 Remove per-RPC ownership cleanup for shared request-created public exchanges while preserving the existing BALANCE, ORDERS, and private request-exchange lifecycle.
- [x] 3.4 Register awaited supervisor retirement with both `CEXBroker.run()` runtime replacement and `stop()`, coordinating configured-primary versus request-created exchange ownership.
- [x] 3.5 Run and extend Subscribe regression coverage to prove BALANCE, ORDERS, secondary account routing, execution/write archival, request-scoped private broker cleanup, and archive-forwarder behavior remain unchanged.

## 4. CEX Proof A, External Proof B Contract, and Local Proof C

- [x] 4.1 Extend the controlled exchange and archive lifecycle harness with ordered L2 event tapes whose level boundaries deliberately cover and fail configured Maker bands, plus acquisition-profile identity, logical subscribe/delivery events, physical CCXT watch iterations, archive decisions, and explicit subscription/frame/duration/flush/spool/query barriers.
- [x] 4.2 Add a production-`MarketDataCollector` E2E comparison runner that feeds identical tapes for a configured minimum duration and frame count into conservative depth-isolated and candidate coalesced broker compositions.
- [x] 4.3 Compare ordered normalized logical payloads per subscription and canonicalized unique archive rows with deployment/run identity removed; fail on missing, reordered, changed, or additional market events while separately asserting fewer coalesced physical watches/archive offers.
- [x] 4.4 Add a band-coverage verifier that requires farthest retained bid/ask prices to cross every Maker boundary or explicit book exhaustion; include a negative case where P retained levels still stop inside the required band and emit venue/profile/band/side diagnostics.
- [x] 4.5 Configure the replay gate with policy depth P and archive depth at least P, rehydrate L2 inputs from ClickHouse, and compare live versus archived mid, band depths, limiting side, and coverage verdict; include a negative 25-level archive case that cannot reproduce a deeper policy.
- [ ] 4.6 Define and publish deterministic UTF-8 JSON CEX Proof A as `cex-orderbook-coalescing-evidence/v1`: existing `canonicalSerialize` bytes plus one LF, whole-file SHA-256, no self-hash/nondeterministic metadata, fixed top-level depth/bands/cases fields, exactly one Binance and MEXC case, candidate profile IDs, ordered conservative/coalesced live and rehydrated observations, coverage and snapshot hashes, CEX verdicts, physical-work counts, and the separate 25-level insufficiency case.
- [ ] 4.7 Enrich the shared policy-visible depth-100 event tape to at least five materially different bid/ask quantity observations, then require independent Binance and MEXC Proof A cases to pass logical/archive equality, band coverage, live/replay input equality, the replay-negative assertion, and reduced physical work without calculating Maker policy outputs.
- [x] 4.8 Extend the four-feed archive lifecycle with overlapping compatible ORDERBOOK collector/client depths and a zero-bootstrap-first OHLCV client followed by the positive-bootstrap collector, proving one physical worker/tracker/archive bootstrap and correct live delivery.
- [ ] 4.9 Make candidate activation fail closed: supply an explicit enabled-profile set to resolver/supervisor composition, default it to empty in production, keep Binance/MEXC conservative when absent, allow scoped candidate enablement only in controlled E2E/sidecar compositions, and prove evidence files cannot toggle runtime behavior.
- [ ] 4.10 Replace the sidecar's projected v1 immediate-hedgeability consumer with a mandatory `profileEvidence.immediateHedgeability` attachment descriptor using `fiet-maker-immediate-hedgeability-attachment/v1`: resolve its run-owned safe path, recompute its SHA, validate attached `fiet-maker-immediate-hedgeability/v2`, require exactly one Binance and MEXC case/profile plus policy/evaluation/hash evidence, bind `sourceCexEvidence.sha256` to freshly generated real Proof A, reject synthetic/stale binding and forbidden copied CEX fields, then combine it with local Proof C without invoking Maker policy.
- [ ] 4.11 Update `SERVICES_ARCHITECTURE.md` and operational verification docs to describe collector coverage/liveness, L2 input evidence, policy-versus-archive depth alignment, candidate versus active venue profiles, the Proof A/B/C ownership split, the empty production enabled-profile set, and the later activation change.

## External Delegated Dependencies

- FIET Maker change `verify-cex-coalesced-orderbook-policy-equivalence` owns four isolated real `Layer12LiveController._author_strategy_policy` evaluations per venue and publishes hash-bound `fiet-maker-immediate-hedgeability/v2`; it is not implemented in this CEX change.
- Maker MAY use a checked-in synthetic Proof A fixture for its isolated CI until the first real CEX Proof A sample is published, but the final CEX production-compatible verifier MUST require Proof B to bind to freshly generated real Proof A.
- A later CEX change, `activate-binance-mexc-coalesced-orderbook-profiles`, owns pinning accepted Proof A/Proof B hashes and changing the production enabled-profile set after both repositories complete their clean-SHA evidence run.

## 5. Verification

- [ ] 5.1 Re-run the focused resolver, supervisor, Subscribe, order-book, OHLCV, archive, account-stream, broker/runtime-lifecycle, Proof A serializer, candidate-default, and Proof B attachment-validation test files and resolve all failures.
- [ ] 5.2 Re-run the full `bun test test` suite, TypeScript/build checks, and Biome checks with no regressions after the ownership/default-off amendment.
- [ ] 5.3 Run the integrated archive lifecycle and independent Binance/MEXC conservative-versus-candidate collector gates, retain real Proof A and its hash, obtain Maker Proof B v2 bound to that exact hash, and run the production-compatible CEX verifier over A+B+local C, retaining separate evidence for logical/archive equality, band coverage, replay sufficiency and 25-level rejection, reduced physical work, real Maker broker overlap, successful strategy spool drainage, and canonical ClickHouse output.
