# Canonical-only amendment GREEN evidence

Recorded 2026-08-03 UTC after integrating corrected prerequisite commit `2730a00a0fcd6cbafbcb03cb432fa7f4224d269a` and removing the obsolete write-mode and credential-classification surfaces. This evidence supersedes the pre-amendment GREEN results.

- Prerequisite integrity: `2730a00` is an ancestor of the current branch. Its `canonical-cex-market-data-replay-archive` OpenSpec artifacts have no diff from the prerequisite commit.
- Configuration contract: the E2E repository audit passed across production code, schema, scripts, E2E support, tests, workflows, docs, `README.md`, `package.json`, and `.env.sample`. The only retained forbidden-token occurrence is the corrected prerequisite's negative migration assertion. No removed write-mode, credential policy/profile, attestation, or smoke-secret control remains executable.
- Required E2E: `bun run test:e2e:archive` passed 9/9 tests with 48 assertions against checksum-pinned ClickHouse Local `25.8.24.21`. It covered production schema execution, all 15 historical tables, the canonical-only four-feed lifecycle, order-book duplicates/conflicts, blocked-sink isolation, retry, and exact terminal loss accounting.
- History/download independence: the same E2E command passed 9/9 from a Git-free source-only temporary export after `bun install --frozen-lockfile --offline`, using only the committed fixture and exact-version `CLICKHOUSE_LOCAL_BIN` override. The temporary export was moved to system trash after the run.
- Immutable baseline: the earlier task-10.1 regeneration checks passed twice with fixture SHA-256 `560e7fb5f38047d155d49c1df68aa3d158ffe37760935feb3bb11cbb3b4f8a66`. The canonical-only amendment leaves every committed baseline input, projection, expected row, and provenance file unchanged.
- Normal tests: `bun run test` passed 550/550 tests with 1,540 assertions. The archive E2E runner remains outside this command. The public-smoke operation guard passed as part of this run.
- Server-backed integration boundary: `bun test test/clickhouse-schema.integration.test.ts` completed 10/10 tests. No ClickHouse server was provisioned locally, so the initial `@clickhouse/client` transport probe reported `ECONNREFUSED`; the retained suite remains the CI/operator-owned production network boundary.
- Static/build validation: `bun run check`, `bun run check:server-lines`, `bun run build:ts`, and `bun run build` passed. Biome reports existing warnings but no errors, `src/server.ts` is 61 lines, and build regeneration leaves no protobuf source or descriptor diff.
- OpenSpec: `openspec validate clickhouse-local-archive-e2e-regression --strict` passed.
- Live smoke: `bun run test:smoke:archive` passed against live Binance `BTC/USDT` without exchange credentials. ORDERBOOK, TICKER, TRADES, and OHLCV each produced a raw event linked to normalized canonical storage. The operation inventory contains only public `Subscribe`; no permission probe, private feed, `ExecuteAction`, order, cancellation, transfer, deposit, withdrawal, or other asset-moving path ran.
- Final diff: no production `src/`, `services/`, schema, fixture, protobuf/RPC, or prerequisite-OpenSpec file is modified by the amendment implementation. No E2E skip call is present, and `git diff --check` passes.

Expected warning/error logs in the required E2E are produced only by deliberate source-rejection, checksum-conflict, recoverable-inserter-failure, and terminal-inserter-failure scenarios. The live smoke emits remote WebSocket-close diagnostics during its explicit bounded shutdown; all feed/link assertions and cleanup completed successfully.
