diff --git a/node_modules/@usherlabs/ccxt/.bun-tag-a296b4edbb70d359 b/.bun-tag-a296b4edbb70d359
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@usherlabs/ccxt/.bun-tag-e1eca9fb87d1075f b/.bun-tag-e1eca9fb87d1075f
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/cjs/src/binance.js b/dist/cjs/src/binance.js
index 40b4c4e8989fcbcdc693216cdb0d43bf0f553a88..f16e4357adc4998e8a9013a94f6e4da4e600e180 100644
--- a/dist/cjs/src/binance.js
+++ b/dist/cjs/src/binance.js
@@ -8529,11 +8529,39 @@ class binance extends binance$1 {
             //       }
             //     ]
         }
+        if (!Array.isArray(response)) {
+            throw new errors.BadResponse(this.id + ' fetchWithdrawals() expected a withdrawal array from sapi but received ' + this.describeUnexpectedResponse(response));
+        }
         for (let i = 0; i < response.length; i++) {
-            response[i]['type'] = 'withdrawal';
+            const entry = response[i];
+            if ((entry === undefined) || (entry === null) || (typeof entry !== 'object') || Array.isArray(entry)) {
+                throw new errors.BadResponse(this.id + ' fetchWithdrawals() expected withdrawal objects from sapi but entry ' + i.toString() + ' of ' + response.length.toString() + ' is ' + this.describeUnexpectedResponse(entry));
+            }
+            entry['type'] = 'withdrawal';
         }
         return this.parseTransactions(response, currency, since, limit);
     }
+    describeUnexpectedResponse(value) {
+        // Bounded single-line shape summary for BadResponse diagnostics.
+        // Never the full body: at most the first 64 characters of a string.
+        if (value === undefined) {
+            return 'undefined';
+        }
+        if (value === null) {
+            return 'null';
+        }
+        if (typeof value === 'string') {
+            const head = value.slice(0, 64).replace(/\s+/g, ' ');
+            return 'a string of length ' + value.length.toString() + ' starting with ' + JSON.stringify(head);
+        }
+        if (Array.isArray(value)) {
+            return 'an array of length ' + value.length.toString();
+        }
+        if (typeof value === 'object') {
+            return 'an object with keys ' + JSON.stringify(Object.keys(value).slice(0, 8));
+        }
+        return 'a ' + typeof value + ' ' + String(value).slice(0, 64);
+    }
     parseTransactionStatusByType(status, type = undefined) {
         if (type === undefined) {
             return status;
@@ -12045,7 +12073,7 @@ class binance extends binance$1 {
             if ((api === 'sapi') && (path === 'asset/dust')) {
                 query = this.urlencodeWithArrayRepeat(extendedParams);
             }
-            else if ((path === 'batchOrders') || (path.indexOf('sub-account') >= 0) || (path === 'capital/withdraw/apply') || (path.indexOf('staking') >= 0) || (path.indexOf('simple-earn') >= 0)) {
+            else if ((path === 'batchOrders') || (path.indexOf('sub-account') >= 0) || (path === 'capital/withdraw/apply') || (path === 'localentity/withdraw/apply') || (path === 'localentity/deposit/provide-info') || (path.indexOf('staking') >= 0) || (path.indexOf('simple-earn') >= 0)) {
                 if ((method === 'DELETE') && (path === 'batchOrders')) {
                     const orderidlist = this.safeList(extendedParams, 'orderidlist', []);
                     const origclientorderidlist = this.safeList(extendedParams, 'origclientorderidlist', []);
diff --git a/js/src/binance.js b/js/src/binance.js
index 9a472db82ff35f67c16bfde11b34831f503d00f9..b10c708beb6c5cf8297135da5838b843514a20d2 100644
--- a/js/src/binance.js
+++ b/js/src/binance.js
@@ -8532,11 +8532,39 @@ export default class binance extends Exchange {
             //       }
             //     ]
         }
+        if (!Array.isArray(response)) {
+            throw new BadResponse(this.id + ' fetchWithdrawals() expected a withdrawal array from sapi but received ' + this.describeUnexpectedResponse(response));
+        }
         for (let i = 0; i < response.length; i++) {
-            response[i]['type'] = 'withdrawal';
+            const entry = response[i];
+            if ((entry === undefined) || (entry === null) || (typeof entry !== 'object') || Array.isArray(entry)) {
+                throw new BadResponse(this.id + ' fetchWithdrawals() expected withdrawal objects from sapi but entry ' + i.toString() + ' of ' + response.length.toString() + ' is ' + this.describeUnexpectedResponse(entry));
+            }
+            entry['type'] = 'withdrawal';
         }
         return this.parseTransactions(response, currency, since, limit);
     }
+    describeUnexpectedResponse(value) {
+        // Bounded single-line shape summary for BadResponse diagnostics.
+        // Never the full body: at most the first 64 characters of a string.
+        if (value === undefined) {
+            return 'undefined';
+        }
+        if (value === null) {
+            return 'null';
+        }
+        if (typeof value === 'string') {
+            const head = value.slice(0, 64).replace(/\s+/g, ' ');
+            return 'a string of length ' + value.length.toString() + ' starting with ' + JSON.stringify(head);
+        }
+        if (Array.isArray(value)) {
+            return 'an array of length ' + value.length.toString();
+        }
+        if (typeof value === 'object') {
+            return 'an object with keys ' + JSON.stringify(Object.keys(value).slice(0, 8));
+        }
+        return 'a ' + typeof value + ' ' + String(value).slice(0, 64);
+    }
     parseTransactionStatusByType(status, type = undefined) {
         if (type === undefined) {
             return status;
@@ -12048,7 +12076,7 @@ export default class binance extends Exchange {
             if ((api === 'sapi') && (path === 'asset/dust')) {
                 query = this.urlencodeWithArrayRepeat(extendedParams);
             }
-            else if ((path === 'batchOrders') || (path.indexOf('sub-account') >= 0) || (path === 'capital/withdraw/apply') || (path.indexOf('staking') >= 0) || (path.indexOf('simple-earn') >= 0)) {
+            else if ((path === 'batchOrders') || (path.indexOf('sub-account') >= 0) || (path === 'capital/withdraw/apply') || (path === 'localentity/withdraw/apply') || (path === 'localentity/deposit/provide-info') || (path.indexOf('staking') >= 0) || (path.indexOf('simple-earn') >= 0)) {
                 if ((method === 'DELETE') && (path === 'batchOrders')) {
                     const orderidlist = this.safeList(extendedParams, 'orderidlist', []);
                     const origclientorderidlist = this.safeList(extendedParams, 'origclientorderidlist', []);
